type Audio
  @auth(rules: [{ allow: owner, identityField: "sub", queries: [list] }])  
  @model(
    subscriptions: { level: off }
    queries: { list: "listAudios", }
  ) {
    id: ID!
    s3key: String!
    subtitlekey: String
    owner: String
  }